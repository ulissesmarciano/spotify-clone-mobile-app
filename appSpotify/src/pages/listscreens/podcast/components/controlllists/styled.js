import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        margin: 15,
    },

    controllContainer: {
        flexDirection: 'row',
        marginTop: 15,
    },

    buttonStyle: {
        color: 'white',
        fontSize: 16,
        fontWeight: 'bold',
        marginRight: 20,
    },
});